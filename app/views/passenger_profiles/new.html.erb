<% content_for :title, "New passenger profile" %>

<div class="container mx-auto px-4 py-8">
  <div class="max-w-3xl mx-auto">
    <div class="bg-white rounded-lg shadow-sm p-6 md:p-8">
      <h1 class="font-bold text-2xl md:text-4xl mb-6">New passenger profile</h1>

      <%= render "form", passenger_profile: @passenger_profile %>

      <%= link_to "Back to dashboard", 
          root_path, 
          class: "mt-4 inline-block rounded-lg px-4 py-2 bg-gray-100 hover:bg-gray-50 text-gray-700 font-medium transition-colors" %>
    </div>
  </div>
</div>
