<% content_for :title, "New booking" %>

<div class="md:w-2/3 w-full p-10">
  <h1 class="font-bold text-4xl">New booking</h1>

  <%= render "form", booking: @booking %>

  <%= link_to "Back to Dashboard", dashboard_path, class: "ml-2 rounded-md px-3.5 py-2.5 bg-gray-100 hover:bg-gray-50 inline-block font-medium" %>
</div>
