<div class="max-w-2xl mx-auto p-6 bg-white">
  <div class="text-center mb-6">
    <%# Add your logo here if you have one %>
    <h1 class="text-2xl font-bold text-gray-800">Password Reset Request</h1>
  </div>

  <div class="space-y-4 text-gray-600">
    <p class="text-lg">Hi <%= @resource.email %>,</p>

    <p>We received a request to reset your password. Don't worry, we've got you covered! You can set a new password by clicking the button below:</p>

    <div class="text-center my-8">
      <%= link_to 'Reset My Password', edit_password_url(@resource, reset_password_token: @token), 
          class: "inline-block px-6 py-3 bg-blue-600 hover:bg-blue-700 text-white font-medium rounded-lg transition-colors duration-200" %>
    </div>

    <div class="text-sm text-gray-500 space-y-2">
      <p>⚠️ If you didn't request this password reset, you can safely ignore this email - no changes will be made to your account.</p>
      <p>🔒 For security: This link will expire in 24 hours and can only be used once.</p>
    </div>

    <hr class="my-6 border-gray-200">

    <div class="text-sm text-gray-500">
      <p>If you're having trouble clicking the button, copy and paste this URL into your browser:</p>
      <p class="mt-2 break-all font-mono text-xs">
        <%= edit_password_url(@resource, reset_password_token: @token) %>
      </p>
    </div>
  </div>
</div>
