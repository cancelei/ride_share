<!DOCTYPE html>
<html>
<head>
  <meta content='text/html; charset=UTF-8' http-equiv='Content-Type' />
  <style>
    body {
      font-family: Arial, sans-serif;
      line-height: 1.6;
      color: #333;
      max-width: 600px;
      margin: 0 auto;
    }
    .container {
      padding: 20px;
      border: 1px solid #e5e7eb;
      border-radius: 8px;
    }
    .header {
      background-color: #7c3aed;
      color: white;
      padding: 15px;
      text-align: center;
      border-radius: 8px 8px 0 0;
      margin: -20px -20px 20px;
    }
    .footer {
      margin-top: 30px;
      font-size: 12px;
      color: #6b7280;
      text-align: center;
    }
    .button {
      display: inline-block;
      background-color: #4f46e5;
      color: white;
      text-decoration: none;
      padding: 10px 20px;
      border-radius: 4px;
      margin-top: 15px;
    }
    .info-box {
      background-color: #f3f4f6;
      padding: 15px;
      border-radius: 4px;
      margin: 15px 0;
    }
  </style>
</head>
<body>
  <div class="container">
    <div class="header">
      <h1>Please Rate Your <%= recipient_type == 'passenger' ? 'Driver' : 'Passenger' %></h1>
    </div>
    
    <p>Hi <%= recipient.first_name %>,</p>
    
    <p>Your ride has been completed. We'd love to hear about your experience!</p>
    
    <div class="info-box">
      <p><strong>Ride Details:</strong></p>
      <p><strong>From:</strong> <%= @ride.pickup_address %></p>
      <p><strong>To:</strong> <%= @ride.dropoff_address %></p>
      <p><strong>Date:</strong> <%= @ride.end_time&.strftime("%B %d, %Y at %I:%M %p") %></p>
      <% if recipient_type == 'passenger' %>
        <p><strong>Driver:</strong> <%= @ride.driver.user.full_name %></p>
      <% else %>
        <p><strong>Passenger:</strong> <%= @ride.passenger.user.full_name %></p>
      <% end %>
    </div>
    
    <p>Your feedback helps us improve our service and helps other users make informed decisions.</p>
    
    <p style="text-align: center;">
      <a href="<%= @url %>/rides/<%= @ride.id %>/ratings/new" class="button">Rate Your Experience</a>
    </p>
    
    <p>Thank you for using RideFlow!</p>
    
    <div class="footer">
      <p>© <%= Date.today.year %> RideFlow. All rights reserved.</p>
      <p>If you have any questions, please contact our support at support@rideflow.live</p>
    </div>
  </div>
</body>
</html> 